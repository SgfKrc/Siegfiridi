[build-system]
requires = ["setuptools>=68,<81"]
build-backend = "setuptools.build_meta"

[project]
name = "siegfridi"
version = "0.1.0"
description = "A Python-first, style-focused MIDI arrange workstation."
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "PySide6>=6.8",
    "mido>=1.3,<2",
    "python-rtmidi>=1.5,<2",
]

[project.optional-dependencies]
audio = [
    "av>=14",
    "librosa>=0.10,<1",
    "numpy>=1.26.4,<2",
    "scipy>=1.14,<2",
    "soundfile>=0.12,<1",
]
transcription = [
    "basic-pitch>=0.4,<1",
    "numpy>=1.26.4,<2",
    "onnxruntime>=1.18,<2",
    "resampy>=0.4.2,<0.4.3",
    "setuptools>=68,<81",
    "tensorflow>=2.14,<2.15.1; platform_system == 'Windows' and python_version >= '3.11'",
]
synthesis = [
    "numpy>=1.26.4,<2",
    "pyfluidsynth>=1.3,<2",
]
dev = [
    "coverage>=7,<8",
    "pyinstaller>=6,<7",
    "pytest>=8,<9",
    "ruff>=0.8,<1",
]
assets = [
    "py7zr>=1,<2",
]
all = [
    "av>=14",
    "librosa>=0.10,<1",
    "numpy>=1.26.4,<2",
    "scipy>=1.14,<2",
    "soundfile>=0.12,<1",
    "basic-pitch>=0.4,<1",
    "onnxruntime>=1.18,<2",
    "pyfluidsynth>=1.3,<2",
    "resampy>=0.4.2,<0.4.3",
    "setuptools>=68,<81",
    "tensorflow>=2.14,<2.15.1; platform_system == 'Windows' and python_version >= '3.11'",
    "pyinstaller>=6,<7",
    "pytest>=8,<9",
    "ruff>=0.8,<1",
]

[project.scripts]
siegfridi = "siegfridi.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
