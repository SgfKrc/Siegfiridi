# 立项计划：Siegfridi - MIDI 音轨合成器（曲目 Arrange 编曲工具）

| 项目代号 | Siegfridi（可调整） |
| --- | --- |
| 文档版本 | v0.3（分期开发与一期实施稿） |
| 文档日期 | 2026-08-27 |
| 调研截止 | 2026-08-27，版本和许可证以发布时锁定的依赖清单为准 |
| 立项目标 | 从 0 到 1 交付一款面向定制化音轨生产的 Windows 桌面 MIDI 编曲工具：导入音频并生成可编辑的 MIDI 草稿，围绕东方 Project 风格与黑暗哥特/复古 RPG 审美进行修正、配器和导出 |

## 0. 本次修订结论

1. **采用 Python-first 桌面架构**：应用界面、工程模型、任务编排、MIDI 和音频处理全部由 Python 负责；桌面 UI 使用官方 Qt for Python（PySide6），不再引入 Electron、Node.js、React 或浏览器内转录。
2. **采用单应用 + 工作进程边界**：MVP 不启动 FastAPI 本地服务。GUI 主进程只负责交互和调度，Basic Pitch、音频分离等耗时任务通过 `QProcess`/`multiprocessing` 工作进程运行，避免阻塞 Qt 事件循环。
3. **复用成熟库，不重写 DSP**：Basic Pitch、librosa、PyAV、Mido、python-rtmidi、FluidSynth 等作为组件依赖；项目自研重点是钢琴卷帘、工程模型、转录结果映射和人工修正体验。
4. **收紧音频转录承诺**：Basic Pitch 官方说明其效果在“单次处理一个乐器”时最好，因此 MVP 先承诺单旋律和隔离乐器音频的高质量草稿；完整混音只承诺生成可人工修正的候选轨，不承诺自动准确拆出所有乐器。
5. **定制化优先**：一期资源优先投入项目专用音色包、音轨角色、风格模板和原创示例曲目；通用 GM 音源的全面扩充、音色编辑器和大规模音源库后置。

“全 Python 后端”在本计划中指**产品代码和运行时编排统一使用 Python**。Qt、FFmpeg、RtMidi、FluidSynth 等底层原生库仍然可以被 Python 绑定或子进程调用，这是复用成熟实现而不是重复造轮子。

---

## 1. 项目背景与动机

- 现有编曲工具（DAW）功能庞大、上手门槛高；面向“曲目 arrange / 消重编曲”的轻量工具稀缺。
- 音乐爱好者和同人作者常见工作流是：导入已有曲目，扒谱或改编，替换配器和音色，再导出自己的版本。其中“从原曲得到可编辑音轨”是最耗时的一步。
- 目标是用**自动音频转录 + 图形化 MIDI 编辑 + 音色合成播放**打通“导入原曲 -> 自动生成草稿 -> 人工修正与再创作 -> 合成渲染导出”。

### 1.1 产品定位与音乐美学（一期）

本项目不是通用 DAW，也不是先做一套覆盖所有乐器的 GM 音源。它首先服务于项目自身的原创曲目和定制化音轨生产，风格参考包括：

- **东方 Project 风格参考**：鲜明、可记忆的主旋律和动机，东方调式或带民族色彩的音阶，较高旋律密度，钢琴/管弦/合成器/打击乐的快速轮换。
- **黑暗哥特风参考**：以《恶魔城》《神之亵渎》的氛围为审美坐标，使用教堂管风琴、合唱、钟琴、拨弦、低音弦乐、失真或颗粒化音色，强调终止感、层次和戏剧性。
- **复古 RPG（含 RPG Maker / BLACK SOULS I-II 语境）参考**：有限通道下的清晰配器、循环段落、芯片/FM/采样混合音色和短动机复用。

上述作品和系列仅作为审美坐标，不复制其旋律、谱面、角色、音色样本、工程文件或其他受保护素材。每个风格模板都必须绑定来源可追溯的原创或已获授权音色资产。

一期的核心产物是“可反复修改的风格化音轨”：同一首曲目可以切换不同音色包和配器模板，而不改变 MIDI 工程本身。

### 本期明确不做

- **Vocal（人声演唱/歌声合成）**：不开发歌声合成和人声调教。人声分离只作为可静音或可提取的素材轨道。
- 专业级混音后期（多段压缩、母带等）。
- 移动端和 Web 在线版；本期只交付 Windows 桌面应用。
- 自动识别完整混音中所有乐器并达到专业级扒谱精度。
- 通用 GM 音源的深度开发（完整乐器覆盖、采样录制、复杂音色编辑器和大规模音源库）；GM 仅作为兼容输出和无定制音色时的占位方案。

---

## 2. 项目目标（SMART）

| # | 目标 | 可量化验收口径 |
| --- | --- | --- |
| G1 | 导入音频并生成可编辑 MIDI 草稿 | 支持 `wav / flac / ogg / mp3`；对 3 分钟内单旋律或隔离乐器样本完成解码、节拍估计、转录和 MIDI 映射 |
| G2 | 提供可用的 MIDI 编曲工作台 | 支持多轨、钢琴卷帘、添加/删除/移动/拉伸/力度编辑、量化、播放、撤销/重做 |
| G3 | 正确播放并导出成品 | 优先使用项目专用音色包播放；无专用音色时回退到 GM SoundFont；支持 Windows MIDI 设备输出、标准 `.mid` 和离线 `.wav` |
| G4 | 形成定制化音轨生产闭环 | 支持音轨角色、风格模板和可替换音色包；POC 至少完成 1 个项目专用音色包、3 个编曲模板和 3 段原创示例片段 |
| G5 | 复用成熟开源能力 | 转录、MIDI、音频解码、合成和设备 IO 均优先采用有明确上游和许可证的开源组件 |
| G6 | 可分发和可维护 | Windows 安装包无需用户安装 Python；依赖版本、模型、SoundFont、原生 DLL、原创/第三方资产和许可证均有清单和可追溯来源 |

### 质量基线

- 在项目自建的 10 首单旋律测试集上，经过统一量化规则后，音高和音符事件 F1 目标不低于 0.90；测试集、阈值和硬件在验收记录中固定。
- 隔离钢琴/吉他复音样本以“可编辑草稿”为目标，单独统计音符 F1 和人工修正时间；不把该指标外推到任意完整混音。
- 完整混音转录显示置信度和来源轨道，低置信度事件必须可批量筛选、删除或重新量化。
- 风格化验收以 3 段原创示例片段为基准，检查模板切换后音轨角色、音域、力度曲线和音色映射保持一致；不以“听起来像某一部商业作品”作为验收标准。

---

## 3. 功能范围

### 3.1 核心功能（MVP）

1. **音频导入与解码**：支持 `wav / flac / ogg / mp3`，由 Python 调度 PyAV 或随包 FFmpeg；支持拖拽导入、波形概览和试听。
2. **自动转录（audio-to-MIDI）**：
   - 估算 BPM 和拍点，允许用户修改首拍与速度；
   - 单旋律音高提取；
   - 对隔离乐器运行 Basic Pitch 复音转录；
   - 对完整混音生成“候选轨”，不自动声称已正确区分旋律、和弦、贝斯和鼓；
   - 后处理包含量化、最小音长过滤、音域限制、置信度标记和人工确认。
3. **钢琴卷帘编曲工作台**：
   - 多轨管理（命名、颜色、静音、独奏、音量、声像）；
   - 音符绘制、拖移、拉伸、删除、力度编辑、量化和移调；
   - 复制粘贴、选区、撤销/重做；
   - 速度、拍号和小节网格；
   - 大量音符下的视口裁剪和增量重绘。
4. **合成与播放**：
   - 优先使用项目专用 FluidSynth + SoundFont 音色包；没有定制音色时回退到 GM SoundFont；优先以独立进程方式调用，降低 LGPL 动态链接和 DLL 打包复杂度；
   - 支持用户加载 `.sf2 / .sf3`，并保存音色包版本、哈希和许可证信息；
   - 通过 Mido + python-rtmidi 枚举并输出到 Windows MIDI 设备，如 Microsoft GS Wavetable Synth 或用户安装的虚拟 MIDI 端口；
   - 播放、暂停、停止、定位和轨道静音状态必须与编辑器状态一致。
5. **定制化音色与音轨配置**：
   - 以 `SoundProfile` 描述音轨角色到 bank/program、音域、力度曲线、八度偏移、演奏法和可选 key switch 的映射；
   - 以风格模板保存速度范围、拍号、调式建议、常用轨道、配器密度、循环段落和默认效果参数；
   - 音色包与 MIDI 工程分离，允许在不改音符的情况下切换东方风、黑暗哥特风和复古 RPG 风格配置；
   - 通用 GM 只提供预览和兼容导出，不作为一期音色设计目标。
6. **导出**：标准 MIDI 文件（`.mid`）；使用同一工程状态通过当前选择的 SoundFont 离线渲染为 `.wav`；导出前显示音色包、采样率和轨道信息。

### 3.2 二期候选

- Spleeter 或其他经许可证审查的 stem 分离，用于人声/伴奏、鼓、贝斯等素材预处理；
- 鼓轨专用转录、和弦标签、五线谱视图；
- 通用 GM 音源深度开发、全量采样录制和内置音色编辑器；
- 工程文件（`.siegfridi`）的版本迁移、自动备份和多项目管理；
- 可插拔转录模型（ONNX）和离线模型管理器。

---

## 4. 技术方案

### 4.1 总体架构（推荐方案）

```text
┌──────────────────────────── Siegfridi.exe ────────────────────────────┐
│ Python 主进程                                                         │
│  PySide6 Qt Widgets                                                   │
│   ├─ 主窗口、轨道/混音面板、导入向导、播放控制                         │
│   └─ QGraphicsView 钢琴卷帘（视口裁剪、缩放、网格和编辑命令）           │
│  Application/Core                                                      │
│   ├─ 工程模型：Python dataclasses，时间统一使用 tick/PPQ               │
│   ├─ 命令栈：编辑操作、撤销/重做、工程序列化                            │
│   ├─ MIDI：Mido 为文件/端口边界，pretty_midi 仅作分析辅助               │
│   └─ Playback：MIDI 时钟、SoundProfile、设备路由和状态同步              │
│                              │ QProcess / multiprocessing             │
│  Python 工作进程                                                        │
│   ├─ 解码/预处理：PyAV 或 FFmpeg、NumPy、SciPy、librosa                 │
│   ├─ 转录：Basic Pitch（Windows 优先 ONNX Runtime）                     │
│   └─ 可选分离：Spleeter；Demucs 仅研究验证，不随包                       │
├──────────────────────────────────────────────────────────────────────┤
│ 原生依赖（由 Python 绑定或受控子进程调用）                              │
│   FFmpeg / FluidSynth / RtMidi / Qt                                     │
└──────────────────────────────────────────────────────────────────────┘
```

**架构决策**

- **桌面 UI 选择 PySide6**：官方 Qt Python 绑定，适合 Windows 桌面、文件系统、菜单、快捷键和高密度编辑器；`QGraphicsView` 提供场景坐标、缩放、缓存和交互能力，适合作为钢琴卷帘基础。
- **不采用 Electron**：本项目不需要浏览器渲染能力，改用 PySide6 可以删除 Node.js、IPC、Chromium 和前后端双运行时，减少安装包和调试面。
- **不采用本地 FastAPI 服务作为 MVP 边界**：GUI 与转录逻辑都在 Python，服务化只会增加端口、生命周期和打包问题。需要隔离 GIL、崩溃或 GPU 任务时使用工作进程；未来若要开放外部 API，再单独引入 FastAPI。
- **主进程不运行重模型和长音频处理**：所有任务都返回结构化结果、进度、日志和可恢复错误，UI 可取消任务并保留上次成功结果。
- **单一内部音乐数据模型**：以 tick/PPQ 保存事件，秒数只在播放、渲染和转录边界转换，避免 Mido、pretty_midi 和 UI 各自维护一套时间基准。
- **音色与工程解耦**：MIDI 工程只保存轨道角色和音色包引用，不把采样数据硬编码进音符事件；风格模板和音色包可以独立版本化、替换和回滚。
- **定制音色优先、通用音源后置**：FluidSynth 作为稳定的播放/渲染容器，先服务项目专用 SF2/SF3；GM 音源只做回退，不投入完整音色库研发。

### 4.2 Python 技术栈

| 层 | 选型 | 用法与边界 |
| --- | --- | --- |
| GUI | PySide6 / Qt Widgets | 主窗口、快捷键、模型视图、`QGraphicsView` 钢琴卷帘；不依赖 QML/JavaScript |
| 工程状态 | Python `dataclasses` + 命令栈 | 音轨、事件、tempo、拍号、SoundFont 配置；所有变更经过 command，便于撤销和测试 |
| MIDI 文件与端口 | Mido + `python-rtmidi` | Mido 负责消息、文件和端口抽象；RtMidi 的 Windows `winmm` 后端负责系统 MIDI IO |
| MIDI 分析辅助 | pretty_midi | 仅在导入、时间换算或分析需要时使用；不与内部模型并存 |
| 音频解码 | PyAV（BSD-3-Clause）或随包 FFmpeg | PyAV 提供 Python 访问 FFmpeg 容器/帧；发布包必须记录 FFmpeg 构建选项和许可证 |
| 音频分析 | librosa + NumPy + SciPy | 重采样、频谱、起音和基础节拍跟踪；耗时步骤在工作进程 |
| 转录 | Spotify Basic Pitch（Apache-2.0，Windows 优先 ONNX） | 单乐器和隔离 stem 优先；保留 note events、pitch bend、置信度和模型版本 |
| 音色资产 | 项目专用 SF2/SF3 + `SoundProfile`/风格模板 JSON | 采样、映射、效果参数和来源可独立替换；禁止把未审查商业素材提交到仓库 |
| 合成与渲染 | FluidSynth + pyFluidSynth 或 FluidSynth CLI | POC 可用绑定；发布版优先独立进程，输出 `.wav` 并将当前音色包来源写入清单 |
| 打包 | PyInstaller（Windows 原生构建） | 生成安装包和可选便携包；为 Qt 插件、模型、SoundFont、FFmpeg、FluidSynth 编写显式收集规则 |

建议锁定 Python 3.11.x：Basic Pitch 的 Windows ONNX 路线和当前 PyAV Windows wheel 均有清晰支持路径；具体版本在 POC 阶段按实际依赖解析结果固定到 `pyproject.toml`/锁文件。

### 4.3 后端方案对比

| 方案 | 优势 | 劣势 | 结论 |
| --- | --- | --- | --- |
| **A. PySide6 + Python 核心 + Python 工作进程** | 单一语言和运行时；共享工程模型；桌面能力完整；适合离线应用 | 需要自行处理 Qt 绘制和进程通信；Python 绑定仍依赖原生库 | **采用** |
| B. PySide6 + FastAPI 本地服务 | 业务边界清晰，未来可复用 API | 端口、服务生命周期、错误恢复和打包复杂；MVP 没有网络需求 | 不采用，未来按需引入 |
| C. Electron + Node.js + Python 服务 | Web UI 生态成熟，前端招聘容易 | 两套运行时和 IPC；体积大；与 Python 偏好冲突 | 不采用 |
| D. C++/Qt 或 Rust 音频核心 | 实时性能上限高 | 开发成本、跨语言接口和维护成本明显增加 | 不作为 MVP；仅在性能剖析后局部替换 |

### 4.4 音频识别与映射流程

```text
导入 wav/flac/ogg/mp3
      │ Python 任务调度 -> PyAV/FFmpeg 解码、采样率和声道检查
      ▼
预处理：归一化、单声道副本、分段、缓存和输入指纹
      │
      ├─ 节拍：librosa beat_track -> BPM/beat -> 用户确认拍线
      │
      ├─ 单旋律：基础音高跟踪（POC 阶段比较 librosa/pYIN 等方案）
      │
      └─ 隔离乐器/候选 stem：Basic Pitch -> note events、pitch bend、置信度
              │
              ▼
后处理：音域限制、最小音长过滤、量化、重叠音符修正、轨道命名建议
      │
      ▼
统一内部模型（tick/PPQ）-> 绑定轨道角色/SoundProfile -> 用户确认/批量修正 -> 钢琴卷帘
      │
      ├─ Mido 写出标准 MIDI
      └─ 当前风格音色包 + FluidSynth 离线渲染 WAV
```

**重要限制**：Basic Pitch 支持复音，但官方建议一次处理一个乐器。完整混音路径应先将音频分段或分离为候选 stem，再分别转录；分离失败时仍要允许用户直接转录并明确标注“混音候选轨”。鼓、贝斯和和弦的自动分类列为增强能力，不作为 MVP 的准确性承诺。

### 4.5 开源资源调研与复用决策

| 资源 | 定位与调研结论 | 许可证/分发注意 | 决策 |
| --- | --- | --- | --- |
| [Qt for Python / PySide6](https://doc.qt.io/qtforpython-6/) | 官方 Qt Python 绑定，提供 Qt Widgets 和 Graphics View，适合桌面编辑器 | LGPLv3/GPLv3 或商业许可；发布时随包提供许可和第三方声明，避免修改 Qt 二进制 | 直接采用 |
| [Basic Pitch](https://github.com/spotify/basic-pitch) | Spotify 的轻量 AMT；提供 Python API、CLI、MIDI/音符事件和 Windows ONNX 路线；单乐器效果最好 | 源码 Apache-2.0；模型文件、TensorFlow/ONNX 运行时和依赖仍需在发布清单中核验 | MVP 主转录 |
| [librosa](https://librosa.org/) | Python 音乐信息检索工具，提供重采样、频谱、起音和 `beat_track` | ISC；依赖 NumPy/SciPy 等，按锁文件审查 | 节拍和预处理 |
| [Mido](https://github.com/mido/mido) | Python MIDI 消息、文件、端口和播放 API，MIT | MIT，需保留版权声明 | MIDI 边界 |
| [python-rtmidi](https://github.com/SpotlightKid/python-rtmidi) | RtMidi 的 Python 绑定，可使用 Windows `winmm` 后端访问系统 MIDI 端口 | MIT；RtMidi 本身也带许可证和版权声明 | 外部 MIDI IO |
| [pretty_midi](https://github.com/craffel/pretty-midi) | MIDI 创建、编辑、分析和格式转换辅助库 | MIT；维护节奏和新 setuptools 兼容性在 POC 中验证 | 辅助，不作为核心模型 |
| [PyAV](https://github.com/PyAV-Org/PyAV) | Pythonic FFmpeg 绑定，Windows 提供二进制 wheel，适合容器/帧级访问 | PyAV 为 BSD-3-Clause；FFmpeg 的实际构建选项可能引入 LGPL/GPL 义务 | 解码候选 |
| [FluidSynth](https://github.com/FluidSynth/fluidsynth) / [pyFluidSynth](https://github.com/amberwhitehead/pyfluidsynth) | 成熟 SoundFont 软件合成器；可实时播放，也可离线渲染 WAV | FluidSynth 和 pyFluidSynth 为 LGPL 系列；优先独立进程，附带许可证和可替换 DLL | 合成与渲染 |
| [Polyphone](https://github.com/davy7125/polyphone) | 开源 SoundFont 编辑器，可将自录或获授权样本组织为 SF2/SF3，并编辑分区、循环和映射 | Polyphone 应用为 GPL-3.0；作为外部创作工具使用，不嵌入发行包；每个样本仍需单独核验权利 | 音色制作参考 |
| [Spleeter](https://github.com/deezer/spleeter) | Python stem 分离，提供 2/4/5 stem 模型；可作为预处理尝试 | 代码为 MIT；模型和 TensorFlow 运行时单独核验，项目较旧 | 二期可选 |
| [Demucs](https://github.com/facebookresearch/demucs) | 分离质量有参考价值，但官方仓库已于 2025-01-01 归档且不再维护 | 代码 MIT；预训练权重可能有更严格的非商业条款，不能默认随包 | 仅研究验证，不进入 MVP |
| [aubio](https://github.com/aubio/aubio) | 可做起音、节拍和音高检测 | GPL-3.0，可能改变闭源分发策略 | 不作为默认依赖 |
| LMMS / MuseScore / Audacity | 功能矩阵、钢琴卷帘、音频导入和用户交互的成熟参照 | 多为 GPL/C++/Qt，直接移植会变成重写和许可证耦合 | 只借鉴交互和测试思路，不 fork 整机 |

### 4.6 需要自研的部分

- 钢琴卷帘：基于 `QGraphicsView` 的网格、虚拟化/裁剪、音符命中测试、拖拽和缩放。
- 统一工程模型和 command undo/redo：保证 UI、播放和导出使用同一份 tick/PPQ 数据。
- 转录结果映射：stem/候选轨、置信度筛选、批量量化、人工确认和错误恢复。
- 播放时钟和状态同步：编辑器定位、MIDI 输出、停止时的 note-off 清理。
- `SoundProfile`、风格模板、音色包清单和轨道角色到 MIDI program/key switch 的映射。
- 工程文件、缓存目录、模型/SoundFont 版本记录和第三方许可证清单。

---

## 5. 工程结构与质量策略

建议的 Python 包结构（采用 `src` 布局）：

```text
src/siegfridi/
  app/            # PySide6 窗口、视图模型、快捷键
  core/           # dataclasses、tick/PPQ、命令栈、工程序列化
  midi/           # Mido 适配、设备枚举、导入导出
  audio/          # PyAV/FFmpeg、预处理、波形和缓存
  transcription/  # Basic Pitch、节拍、结果映射
  playback/       # FluidSynth、SoundFont、MIDI 时钟
  sound/          # SoundProfile、风格模板、音色包清单
  workers/        # 可取消的 QProcess/multiprocessing 入口
  resources/      # 默认配置，不直接提交未审查模型和音色
tests/
  fixtures/       # 小型、可再分发或自制音频/MIDI 样本
assets/
  packs/          # 仅存放已授权或自制音色包；大型资产不直接入库
  presets/        # 风格模板和示例工程
```

- 单元测试覆盖 tick/秒转换、量化、重叠音符修正、MIDI round-trip 和 command undo/redo。
- 集成测试覆盖“导入 -> 转录 -> 映射 -> 导出 MIDI -> 再导入”的数据一致性。
- Windows CI 或专用构建机验证 PySide6、RtMidi、FluidSynth、FFmpeg DLL 和 PyInstaller 包。
- 记录每次模型运行的输入哈希、模型版本、参数、耗时和错误；转录结果可复现。
- 发布前生成依赖锁文件、SBOM、NOTICE/许可证目录和模型/SoundFont 来源清单。
- 风格验收使用原创片段和项目自制/已授权音色；不得将商业作品的采样、旋律、谱面或拆包资源作为测试夹具。

---

## 6. 里程碑与排期（建议，单人全职口径）

| 阶段 | 周期 | 交付物 |
| --- | --- | --- |
| M0 调研与立项确认 | 第 1 周 | PySide6、Mido/RtMidi、Basic Pitch ONNX、PyAV/FFmpeg、FluidSynth 的 Windows POC；确认东方风/黑暗哥特/复古 RPG 的风格 brief、原创示例计划和资产许可证清单 |
| M1 技术验证 POC | 第 2-3 周 | PySide6 窗口；导入/播放 MIDI；系统 MIDI 端口枚举；1 个项目专用音色包 + 3 个风格模板；一首隔离乐器样本完成“导入 -> 转录 -> MIDI” |
| M2 MVP 编辑器 | 第 4-8 周 | 工程模型、钢琴卷帘、多轨、音符编辑、量化、播放、撤销/重做；轨道角色和 SoundProfile 可编辑 |
| M3 转录管线集成 | 第 9-12 周 | 音频解码、节拍、Basic Pitch 工作进程、分段/缓存、置信度和人工确认流程；转录结果可绑定风格模板 |
| M4 合成输出 | 第 13-14 周 | 项目专用 SoundFont/SoundProfile 播放、GM 回退、Windows MIDI 输出、`.mid`/`.wav` 导出 |
| M5 打磨与发布 | 第 15-18 周 | 性能和稳定性、PyInstaller 安装包、许可证/模型/音色包清单、3 段原创示例片段和用户文档 |
| **合计** | **约 4-4.5 个月** | 可发布 v1.0 定制化音轨工具；通用音源深度开发和复杂分离能力保留到二期 |

兼职或两人协作建议按 6-8 个月排期。M0 的转录质量和原生依赖验证是前置门槛，若 POC 未达到质量基线，应先收缩目标而不是继续堆 UI 功能。

---

## 6.1 分期开发计划与开发票据

每张开发票据都必须同时具备代码、测试、文档和可复现的验收记录；当前阶段的门禁未通过前，不进入下一阶段。编号用于 issue、分支和提交信息。

| 期次 | 目标 | 开发票据 | 门禁/状态 |
| --- | --- | --- | --- |
| P0 | 立项和技术路线确认 | P0-001 Python/PySide6 方案；P0-002 开源组件与许可证调研；P0-003 风格 brief 和资产规则 | 已完成：立项计划 v0.3 |
| **P1** | **基础纵向切片** | P1-001 仓库与 `src` 包结构；P1-002 tick/PPQ 工程模型；P1-003 `SoundProfile`/风格模板模型；P1-004 MIDI 文件 round-trip；P1-005 PySide6 窗口壳；P1-006 测试和 editable 安装检查 | **已完成**：核心测试通过、`--version` 可运行、音符/轨道元数据往返一致，速度允许标准 MIDI 的微秒表示误差 |
| **P2** | **编曲编辑器** | P2-001 `QGraphicsView` 网格与视口；P2-002 音符命中/拖拽/拉伸；P2-003 command undo/redo；P2-004 轨道面板和基础 MIDI 播放 | **已完成**：钢琴卷帘可编辑，命令可撤销/重做，轨道支持静音/独奏，播放事件可在无硬件环境测试 |
| **P3** | **音频转录管线** | P3-001 PyAV/FFmpeg 解码缓存；P3-002 librosa 节拍；P3-003 Basic Pitch 工作进程；P3-004 置信度和候选轨映射 | **已完成**：固定 `numpy<2`/TensorFlow 2.14 兼容链；合成三音符 WAV 实测生成 3 个候选音符；13 项测试通过 |
| **P4** | **定制音色和渲染** | P4-001 项目专用 SF2/SF3 音色包；P4-002 3 套风格模板；P4-003 FluidSynth 播放/渲染；P4-004 Windows MIDI 设备路由 | **基础实现完成**：清单/哈希校验、3 套风格预设、FluidSynth CLI 与 pyFluidSynth 原生绑定渲染；项目专用音色包仍待制作与授权验收 |
| P5 | 打包与发布 | P5-001 PyInstaller；P5-002 DLL/模型/音色资产清单；P5-003 安装包冒烟测试；P5-004 用户文档和示例工程 | 待开始；门禁为干净 Windows 环境双击启动 |

### P1 第一期开工顺序

1. **先固定边界**：工程数据只使用 tick/PPQ；Mido 是 MIDI 文件边界；音色包只通过 `SoundProfile` 引用。
2. **先完成闭环**：构造 `Project` -> 写出 `.mid` -> 再读回 `Project`，验证速度、轨道角色、音色引用和音符事件保持一致。
3. **再接 UI 壳**：PySide6 只展示版本和工作台入口，不在主线程加载模型或处理长音频。
4. **最后过门禁**：运行核心测试、editable 安装检查和 `python -m siegfridi --version`，记录环境和结果后才进入 P2。

P1 已完成并通过门禁；P2 已完成并通过门禁；P3 已完成并通过实际模型验证；P4 基础实现完成，CLI/原生绑定与 GM SoundFont 已验收，待项目专用音色包与许可证清单完成后进入 P5 打包发布。

### P2 实施记录

- **P2-001/P2-002**：`PianoRollView` 使用 `QGraphicsView` 绘制 tick 网格和 128 个音高行；支持空白处新增音符、命中选择、拖移、右边缘拉伸、右键/`Delete` 删除和 `Ctrl` 缩放。
- **P2-003**：`CommandStack` 统一承载新增、删除、移动、拉伸和力度修改，提供 `Ctrl+Z`/`Ctrl+Y` 撤销与重做。
- **P2-004**：主窗口接入轨道列表、静音/独奏和播放/停止；`MidiPlayer` 生成稳定的 tick 时间线，延迟枚举系统 MIDI 输出，无设备时仍可用回调进行测试。
- **门禁记录**：1 万音符场景刷新约 0.285 秒；无窗口 Qt 启动、工作台播放/停止冒烟测试通过；核心测试共 8 项通过。

### P3 实施记录

- **P3-001**：`decode_audio` 优先调用 PyAV；无 PyAV 时对 WAV 使用标准库回退；`AudioCache` 以源文件指纹和目标采样率缓存单声道浮点 PCM。
- **P3-002**：`estimate_beats` 对接 librosa 节拍跟踪；依赖缺失、空音频或算法异常时回退到可复现的 BPM/拍点序列。
- **P3-003**：`transcribe_file` 封装 Basic Pitch 官方推理入口；`TranscriptionProcess` 使用 Windows `spawn`、队列消息和终止接口隔离模型任务。
- **P3-004**：`CandidateNote`/`TranscriptionResult` 保留置信度、来源和模型版本，支持阈值过滤并映射为现有 tick/PPQ 候选轨。
- **兼容性修复**：Basic Pitch 0.4.0 在 Windows/Python 3.11 使用 TensorFlow `<2.15.1`；当前环境的 NumPy 2.4.6 会触发 TensorFlow ABI 错误，已固定 `numpy>=1.26.4,<2`。Basic Pitch 依赖的 `resampy 0.4.2` 仍导入 `pkg_resources`，已固定 `setuptools>=68,<81`。
- **实际验证**：`numpy 1.26.4`、TensorFlow 2.14.0、Basic Pitch 0.4.0 可导入；3 秒合成三音符 WAV 生成 3 个候选音符（C4/E4/G4），1.5 秒 A4 单音 WAV 生成可映射候选音符；P3 自动化与运行时测试通过。

### P4 实施记录

- **P4-001**：`SoundPackManifest` 读取版本、许可证、SoundFont 相对路径和 SHA-256；渲染前强制校验文件存在与哈希，轨道通过 `SoundProfile` 引用音色。
- **P4-002**：内置 `oriental-project`、`dark-gothic`、`retro-rpg` 三套风格预设，定义速度、拍号、音轨角色和候选音色引用。
- **P4-003/P4-004**：`render_wav` 优先调用 FluidSynth CLI；CLI 不在 PATH 时回退到 pyFluidSynth 原生绑定，通过 `get_samples` 输出标准双声道 16-bit WAV；`SIEGFRIDI_FLUIDSYNTH` 可显式指定 CLI 路径，错误类型为缺少 CLI、绑定、DLL 或 SoundFont 提供可诊断边界，Windows MIDI 端口仍由 P2 的 RtMidi 适配负责。
- **实际验证**：本机 `C:\tools\fluidsynth\bin\fluidsynth.exe` 与 `libfluidsynth-3.dll` 均可用，runtime version 2.6.0；使用 `pretty_midi` 附带 `TimGM6mb.sf2` 分别完成 CLI 和原生绑定 WAV 渲染，输出均为双声道 16-bit、非空且有声。
- **当前限制**：项目专用东方 Project / 黑暗哥特音色包尚未进入仓库，需后续完成原创或获授权样本制作、SoundProfile 映射、许可证登记和哈希锁定后，P4 才能完全关闭。

---

## 7. 团队与资源需求

- **Python/PySide6 工程师 1 人**：负责桌面 UI、钢琴卷帘、工程模型、打包和 Windows 兼容。
- **音频算法/DSP 工程师 1 人（可兼职）**：负责测试集、Basic Pitch 参数、节拍和分段策略、质量评估。
- **作曲/音色设计 1 人（可由产品负责人承担）**：负责风格 brief、原创示例曲目、音轨角色、SoundProfile 和音色包验收。
- **测试 0.5 人**：负责 MIDI 设备矩阵、长音频、异常输入、安装包和回归数据。
- **开发环境**：Windows 10/11 x64；Python 3.11.x；Git；`uv`/venv；C/C++ 运行时；可选 NVIDIA GPU。
- **资源策略**：默认安装包只带最小运行时和可分发的占位音色；项目专用音色包、模型和大体积 SoundFont 支持首次使用时下载或用户导入，并显示版本、哈希和许可证。原创录音、合成音色和第三方样本必须分别登记来源。

---

## 8. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 风格定位发散或过度模仿参考作品 | 产品辨识度和版权风险 | 以“风格特征”而非具体作品为验收对象；所有示例曲目、旋律和音色样本原创或有授权；建立风格 brief 和人工评审记录 |
| 定制 SoundFont 样本质量不足或映射不一致 | 音轨听感不稳定、模板无法复用 | 先做小而完整的项目专用音色包；规定采样率、响度、根音、循环点和命名规范；用 Polyphone 等工具制作，运行自动映射检查 |
| 混音中复音转录准确率不足 | 核心体验不达标 | 把 Basic Pitch 定位为单乐器/隔离 stem 草稿；显示置信度；优先提供批量修正和可回退操作；完整混音只做候选轨 |
| Basic Pitch、Spleeter 等模型依赖大或版本不稳定 | 安装失败、首次运行慢 | Windows POC 先固定 Python/ONNX 路线；模型缓存、校验哈希、离线错误提示；按需下载 |
| PySide6/Qt LGPL 与闭源分发义务 | 合规风险 | 不修改 Qt；保留动态库可替换性；随包提供许可证和源码获取说明；发布前做法律审查 |
| FluidSynth/FFmpeg 原生依赖和许可证复杂 | 播放失败或分发违规 | 优先 FluidSynth 独立进程；固定 FFmpeg 构建；记录 DLL 来源、编译选项和 LGPL/GPL 状态；提供无设备时的可诊断错误 |
| Windows MIDI 设备名称和驱动差异 | 外部合成器不可用 | M0 建立 Microsoft GS Wavetable、虚拟 MIDI、无设备三种测试矩阵；设备热插拔和端口失效要可恢复 |
| Qt 主线程被模型或渲染阻塞 | UI 卡顿、无响应 | 统一工作进程协议；进度/取消/超时；钢琴卷帘只绘制可见区域并做增量更新 |
| PyInstaller 漏收 Qt 插件、模型或 DLL | 安装包在开发机外启动失败 | Windows 原生构建；安装包冒烟测试；显式收集 hidden imports、Qt plugins、模型和 SoundFont |
| 用户导入受版权保护的曲目 | 法律和平台风险 | 定位为个人学习/练习改编；用户协议明确素材授权责任；不上传音频；示例数据使用自制或可再分发素材 |

---

## 9. 验收标准（立项通过后以此结项）

1. Windows 10/11 上双击安装并启动，无需用户安装 Python 或执行命令行。
2. 导入 `wav / flac / ogg / mp3` 各至少一首样例，完成解码、缓存、试听和错误提示。
3. 对固定的单旋律测试集完成节拍估计、转录、量化和 MIDI 映射，达到第 2 节质量基线；完整混音样例输出带置信度的候选轨。
4. 钢琴卷帘支持绘制、移动、伸缩、删除、力度编辑、量化、多轨静音/独奏、撤销/重做，并在 1 万音符工程中保持可操作。
5. 可加载项目专用音色包，绑定轨道角色和 SoundProfile，并在至少 3 个风格模板间切换；没有专用音色时可明确回退到 GM。
6. 项目专用音色播放连续 10 分钟无明显爆音；停止和定位不会遗留悬挂音符；可切换已枚举的 Windows MIDI 输出端口。
7. `.mid` 导出可被至少一种其他 MIDI 软件重新打开；`.wav` 离线渲染与当前工程轨道、速度和所选风格音色包一致。
8. 3 分钟样例在验收硬件上转录总耗时不超过 5 分钟，超时或模型失败时 UI 能取消、重试并保留日志。
9. 安装包包含第三方许可证、模型/SoundFont/音色样本来源和哈希清单；依赖升级需经过回归测试。

---

## 10. 待立项评审确认的决策点

1. **应用形态**：确认 PySide6 Qt Widgets + Python 核心，放弃 Electron 主方案。
2. **进程边界**：确认 MVP 使用 `QProcess`/`multiprocessing` 工作进程，不引入 FastAPI；未来确需外部 API 时再评审。
3. **产品路线**：确认一期围绕东方 Project 风格参考、黑暗哥特风和复古 RPG 音轨开发；通用 GM 音源深度开发后置。
4. **定制资产范围**：确认首个版本投入 1 个项目专用音色包、3 个风格模板和 3 段原创示例片段；商业作品素材不进入仓库或测试集。
5. **转录范围**：确认 MVP 以单旋律和隔离乐器复音为质量目标，完整混音为候选轨；stem 分离、鼓轨和自动乐器分类列入二期。
6. **合成策略**：确认发布版优先 FluidSynth 独立进程；是否提供 pyFluidSynth 直连模式由 POC 的延迟和许可证验证决定。
7. **依赖与许可证**：确认 Python 3.11.x、PyAV/FFmpeg 组合、默认/定制 SoundFont、模型和音频样本的分发策略；发布前完成 LGPL/GPL/模型权重/资产授权审查。
8. **远端仓库**：确认使用 `https://github.com/SgfKrc/Siegfiridi`；初始化时只配置 remote，不自动 push 或覆盖远端内容。
9. **项目代号**：暂用 Siegfridi，可改名。
10. **排期口径**：按单人全职 18 周；兼职或增加分离能力需重新估算。

### 参考入口（调研链接）

- [Qt for Python](https://doc.qt.io/qtforpython-6/)
- [Basic Pitch](https://github.com/spotify/basic-pitch)
- [librosa beat tracking](https://librosa.org/doc/main/api/generated/librosa.beat.beat_track.html)
- [Mido](https://github.com/mido/mido)
- [python-rtmidi](https://github.com/SpotlightKid/python-rtmidi)
- [PyAV](https://github.com/PyAV-Org/PyAV)
- [FluidSynth](https://github.com/FluidSynth/fluidsynth) / [pyFluidSynth](https://github.com/amberwhitehead/pyfluidsynth)
- [Polyphone SoundFont editor](https://www.polyphone.io/)
- [PyInstaller](https://pyinstaller.org/en/stable/)
